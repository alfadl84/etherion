# Local/dev default (SQLite file, no setup needed).
# For production on serverless hosting (Vercel), switch prisma/schema.prisma
# provider to "postgresql" and set this to a hosted Postgres URL
# (Supabase / Neon / Railway), then run `npx prisma migrate deploy`.
DATABASE_URL="file:./dev.db"
